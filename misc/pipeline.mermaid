flowchart TD
    ChatService -- "Audio Stream" --> STT

    subgraph AIPipeline ["AIPipeline"]
        STT -- "Text" --> RAG

        subgraph KnowledgeRetrieval ["Knowledge Retrieval"]
            RAG <--> Database
            RAG <--> VectorDatabase
        end

        RAG -- "Text + Context" --> LLM
        LLM -- "Response Text" --> TTS
    end

    TTS -- "Audio Stream" --> ChatService